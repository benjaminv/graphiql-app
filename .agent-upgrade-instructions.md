hey this works but feeling due some updates, 
#1 graphial core and dependencies
#2 our app,
#2.1 global theming, currently all settings belong to graphiql, our wrapper ui is separated, as we inherit the theming (dark/light) from graphiql core so it is consistent?
#2.2 data store. we have the tabbed ui (outer layer ui, in graphiql core (inner ui) there is buildin tabs for queries), and we manage our tab ui separately from the inner ui too? i.e., tabs are in electron, site headers (important auth data) are in graphiql core? can we store everything in our electron store and make sure it is definitely always reserved not to lose?
#2.3 on outer layer ui tabs, we have right clicks to name/rename, order of each tab, can we also add drag to move, double click to rename, and most importantly, we need double confirm for closing any tab, unless it is empty.
#2.4 the left side bar is original from inner core, how can we improve this so that settings become global (ohter parts is fine as part of current tab), i know the refresh and shortcuts are part of graphiql core. any idea of how to improve these?
#2.5 is it possible to double confirm when Persist headers is getting switched to off, and clear data is clicked?

i really want to find a way so our app has its global settings while reserve the per tab (instance of graphiql) settings. the current ui works but is very confusing. - we solely rely on tab settings, for example theming is per tab, our wrapper ui is separated, can we inherit the theming (dark/light) from graphiql core so it is consistent? 

lastly, we want to keep the graphiql core as is, not to modify it too much, using its data or overriding is fine, so that we can easily update it in the future.
